# Pozwól, że pokażę ci przykład funkcji, którą zamierzam stworzyć, nazywanej
# increment(). Zazwyczaj chcę użyć tej funkcji, aby zwiększyć
# wartość liczby o jeden. Ta funkcja przyjmuje dwa argumenty: „number” i
# „by”, gdzie „number” to cyfra, którą chcę zwiększyć, a „by” to wartość, o którą
# chcę zwiększyć „number”. Napisałem tę funkcję poniżej.
#
# increment <- function(number, by = 1){
#     number + by
# }
#
# Jeśli spojrzysz między nawiasy, zobaczysz, że ustawiłem
# "by" na 1. Oznacza to, że argument "by" będzie miał domyślną
# wartość 1.
#
# Teraz mogę użyć funkcji increment bez podawania wartości dla "by":
# increment(5) wyniesie 6.
#
# Jednak jeśli chcę podać wartość dla argumentu "by", nadal mogę! Wyrażenie
#: increment(5, 2) wyniesie 7.
#
# Napisz funkcję o nazwie "remainder". remainder() przyjmie
# dwa argumenty: "num" i "divisor", gdzie "num" jest dzielone przez "divisor" i
# zwracana jest reszta. Wyobraź sobie, że zazwyczaj chcesz znać resztę,
# gdy dzielisz przez 2, więc ustaw domyślną wartość "divisor" na 2. Upewnij się,
# że "num" jest pierwszym argumentem, a "divisor" jest drugim argumentem.
#
# Wskazówka nr 1: Możesz użyć operatora modulo %%, aby znaleźć resztę.
# Np.: 7 %% 4 daje wynik 3.
#
# Pamiętaj, aby ustawić odpowiednie wartości domyślne! Pamiętaj, aby zapisać ten
# skrypt i wpisać submit() w konsoli po napisaniu funkcji.

remainder <- function(num, divisor) {
  # Napisz swój kod tutaj!
  # Pamiętaj: zostanie zwrócone ostatnie ocenione wyrażenie!
}
